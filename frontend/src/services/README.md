# Frontend API Services

Frontend API modules live here. Use `apiClient.js` for calls to the backend API.

The API base URL comes from `frontend/.env`:

```env
VITE_API_BASE_URL=http://localhost:4000/api
```

Suggested files:

- `authApi.js`
- `salesApi.js`
- `inventoryApi.js`
- `reportsApi.js`
